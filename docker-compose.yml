services:
  app:
    build: .
#    command: >
#      sh -c "python manage.py collectstatic --noinput &&
#             uv run manage.py runserver 0.0.0.0:8000"
    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media
    ports:
          - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=Django_course_project.settings
    depends_on:
      - redis

  redis:
    image: redis:7

#  nginx:
#    image: nginx:latest
#    volumes:
#      - static_data:/app/staticfiles
#      - media_data:/app/media
#      - ./nginx.conf:/etc/nginx/nginx.conf
#    ports:
#      - "80:80"
#    depends_on:
#      - app

volumes:
  static_data:
  media_data: